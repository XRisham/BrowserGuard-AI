# BrowserGuard AI extension

Run `npm install && npm run build`, then load `dist` as an unpacked MV3 extension. Chrome and Edge are supported. The extension never injects page scripts into privileged extension pages; it sends bounded extracted text through message passing. Local processing is default; API mode is opt-in.
