# Backend

Run `pip install -r requirements.txt`, then from repository root run `python -m ml.training.train_text_model`, and start with `uvicorn app.main:app --reload` from this directory. API reference: [../docs/api.md](../docs/api.md).
