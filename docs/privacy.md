# Privacy

BrowserGuard does not collect browsing history, use trackers, or persist webpage text or image payloads. The browser stores only settings and aggregate local activity. API mode is optional and transmits a maximum 10,000-character text excerpt plus configuration required for a decision. The API returns `Cache-Control: no-store` and does not log payloads. Remove local data from Settings at any time.
