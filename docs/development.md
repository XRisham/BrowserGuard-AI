# Development

Install Python dependencies under `backend/`, run model training from repository root, then run `pytest` inside `backend/`. Install Node dependencies under `extension/`; use `npm test`, `npm run lint`, and `npm run build`. Add real models only with documented licenses, consent, validation, threshold analysis, and bias/safety testing.
